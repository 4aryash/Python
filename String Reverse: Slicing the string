"""Reverse a given number and return true if it is the same as the original number"""

s= str(input())         #taking string as user input

def delta(s):
    return s[::-1]      #slicing and returning the string. Same as [n:0:-1], where n is the last element no., 0 is the 1st element no, -1 is move one step backwards. 

mytxt = delta(s)        #calling the function to reverse the input and storing it in a variable mytxt 
print(mytxt)            #printitn the variable mytxt

if mytxt==s:            #Checking if the input number is a palindrome or not.
    print("The string is a Palindrome.")
else:
    print("The String is not a Palindrome")
